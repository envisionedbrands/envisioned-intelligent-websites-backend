/**
 * Alias for the Resend webhook at the shorter /api/webhooks/resend path, so
 * a Resend dashboard entry configured either way keeps working.
 */
export { POST } from "@/app/api/crm/webhooks/resend/route";
