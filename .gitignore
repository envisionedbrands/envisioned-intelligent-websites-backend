node_modules
.next
.env.local
.env*.local
.DS_Store
.claude/
.vercel/

# typescript
*.tsbuildinfo

# OpenNext
.open-next

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
tsconfig.tsbuildinfo

# Backups of env files (e.g. .env.local.bak-2026-08-11) — these hold live secrets.
# ".env*.local" above does not match them because they do not end in ".local".
.env*.bak*
*.bak
